spring:
  application:
    name: db-service

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:8004/eureka
    server:
      waitTimeInMsWhenSyncEmpty: 0
      